language: python

sudo: false

cache: pip

python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "nightly"

script: nosetests -v --with-coverage

notifications:
    irc:
        channels:
            - "chat.freenode.net#punegeeks"
        template:
            - "%{repository_slug}/%{branch}, commit https://github.com/shardulc/phenny/commit/%{commit}: %{message}"
