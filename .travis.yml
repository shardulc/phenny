language: python

sudo: false

cache: pip

python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "nightly"

# uncomment if needed
#install:
#    - pip install -r requirements.txt

script: nosetests -v --with-coverage
